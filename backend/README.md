# Backend

This folder contains the backend part of the project.

Its purpose is to hold the server-side logic that powers the application, such as API endpoints, business logic, data processing, authentication, database access, and integrations with external services.

Keeping the backend code in its own folder helps separate server responsibilities from frontend or other parts of the repository, making the project easier to organize, maintain, and scale over time.
